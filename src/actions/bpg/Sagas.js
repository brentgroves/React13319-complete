import * as types from '../../constants/ActionTypes';
export const View200206 = (
    startDate,
    endDate,
    limit,
    route,
    setSubmittingOff,
  ) => ({
    type: types.VIEW_200206,
    startDate,
    endDate,
    limit,
    route,
    setSubmittingOff,
  });
